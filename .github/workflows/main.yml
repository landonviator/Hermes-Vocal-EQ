name: Build Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          submodules: recursive

      - name: Set up Xcode
        uses: actions/setup-node@v2
        with:
          node-version: '14'

      - name: Save Projucer Project
        run: |
          Projucer/Projucer.app/Contents/MacOS/Projucer --resave HermesVoiceEQ/"Hermes Voice EQ.jucer"

      - name: Build Plugin Mac AU & VST3
        run: |
          cd HermesVoiceEQ/Builds/MacOSX
          xcodebuild -project "Hermes Voice EQ.xcodeproj" -scheme "Hermes Voice EQ - All" -configuration Release

#      - name: Validate Mac AU
#        run: |
#              Pluginval/pluginval.app/Contents/MacOS/pluginval --strictness-level 5 HermesVoiceEQ/Builds/MacOSX/build/Release/Hermes\ Voice\ EQ.component/Contents/MacOS/Hermes\ Voice\ EQ

      - name: Upload Mac AU Artifact
        uses: actions/upload-artifact@v3
        with:
          name: Mac-Hermes-Voice-EQ-AU
          path: HermesVoiceEQ/Builds/MacOSX/build/Release/Hermes\ Voice\ EQ.component/Contents/MacOS/Hermes\ Voice\ EQ

      - name: Upload Mac VST3 Artifact
        uses: actions/upload-artifact@v3
        with:
          name: Mac-Hermes-Voice-EQ-VST3
          path: HermesVoiceEQ/Builds/MacOSX/build/Release/Hermes\ Voice\ EQ.vst3
        