name: Build Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build Plugin
        run: |
          cd ..
          cd ..
          xcodebuild -project "Hermes Voice EQ/Builds/MacOSX/Hermes Voice EQ.xcodeproj" -scheme "Hermes Voice EQ" -configuration Release build

      # Add additional steps for testing, packaging, etc. as needed
